#include <stdio.h>

int main() {
    // Declare variables
    char name[] = "John Doe";
    float salary = 50000.75;

    // Print name and salary
    printf("Name: %s\n", name);
    printf("Salary: %.2f\n", salary);

    return 0;
}