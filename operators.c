#include <stdio.h>

int main() {
    int a=7;
    int b=5;
    int sum=a+b;
    int sub=a-b;
    int mul=a*b;
    int div=a/b;
    int mod=a%b;
    printf("sum of %d and %d is %d\n",a,b,sum);
    printf("sum of %d and %d is %d\n",a,b,sub);
    printf("sum of %d and %d is %d\n",a,b,mul);
    printf("sum of %d and %d is %d\n",a,b,div);
    printf("sum of %d and %d is %d\n",a,b,mod);

    return 0;
}